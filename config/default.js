const config={

    // 启动端口
    port: 8081,

    // 数据库配置
    database: {
        DATABASE: 'blog',
        USERNAME: 'root',
        PASSWORD: 'rootliang',
        PORT: '3306',
        HOST: '127.0.0.1'
    }
};

module.exports=config;